{
  "name": "cjs-use",
  "private": true,
  "dependencies": {
    "typed-events.ts": "file:../.."
  },
  "scripts": {
    "test": "npm install && npx typescript --noEmit --allowJs --checkJs src/main.js"
  }
}
